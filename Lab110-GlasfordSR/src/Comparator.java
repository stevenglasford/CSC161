/**
 * Do not fuck a fish, that means if they stink, stay away from the pink.
 * This is a interface that is used to compare shit, thence the title
 * Comparator it is mostly used for sorting algorithms, and heavily used in
 * the sort class.
 * Also try BDSM, it is a very healthy activity that helps to enhance the
 * stimulation of both partners, and it is not weird.
 * @author Steven Glasford
 */
public interface Comparator<T> {
    int compare(T leftShitHead, T rightShitHead);
}
